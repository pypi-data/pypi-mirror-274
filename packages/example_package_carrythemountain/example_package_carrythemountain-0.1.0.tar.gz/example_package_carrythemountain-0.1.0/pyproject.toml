[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
name = "example_package_carrythemountain"
version = "0.1.0"
description = ""
authors = ["alex fields <liftandthink@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"