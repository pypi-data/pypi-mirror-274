[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "bfgabc"
dynamic = ["version"]
description = ''
readme = "README.md"
requires-python = ">=3.8"
license = "MIT"
keywords = []
authors = [
  { name = "JamesParrott", email = "80779630+JamesParrott@users.noreply.github.com" },
]
classifiers = [
  "Development Status :: 4 - Beta",
  "Programming Language :: Python",
  "Programming Language :: Python :: 3.8",
  "Programming Language :: Python :: 3.9",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: Implementation :: CPython",
  "Programming Language :: Python :: Implementation :: PyPy",
]
dependencies = []

[project.urls]
Documentation = "https://github.com/JamesParrott/bfgabc#readme"
Issues = "https://github.com/JamesParrott/bfgabc/issues"
Source = "https://github.com/JamesParrott/bfgabc"

[tool.hatch.version]
path = "src/bfgabc/__about__.py"

[tool.hatch.envs.types]
extra-dependencies = [
  "mypy>=1.0.0",
]
[tool.hatch.envs.types.scripts]
check = "mypy --install-types --non-interactive {args:src/bfgabc tests}"

[tool.coverage.run]
source_pkgs = ["bfgabc", "tests"]
branch = true
parallel = true
omit = [
  "src/bfgabc/__about__.py",
]

[tool.coverage.paths]
bfgabc = ["src/bfgabc", "*/bfgabc/src/bfgabc"]
tests = ["tests", "*/bfgabc/tests"]

[tool.coverage.report]
exclude_lines = [
  "no cov",
  "if __name__ == .__main__.:",
  "if TYPE_CHECKING:",
]
