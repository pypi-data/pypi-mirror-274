[project]
name = "cvat_sdk_i_digit"
version = "0.0.3"
description = ""
authors = [
    {name = "Шумелев Дмитрий Игоревич", email = "cmit.dima@gmail.com"}
]
dependencies = [
    "cvat-sdk>=2.13.0",
    "pydantic-settings>=2.0.2",
    "passlib>=1.7.4",
    "pandas>=2.2.2"
]



requires-python = ">=3.11"
license = {text = "MIT"}

[tool.setuptools]
py-modules = []

[tool.pdm.dev-dependencies]
dev = [
    "flake8>=6.1.0",
    "mypy>=1.4.1",
    "black>=23.7.0",
]

[tool.pdm.scripts]
i = "pdm install {args}"
i-dev = "pdm install --dev -G test {args}"
start = "pdm run uvicorn app.__main__:app --host 0.0.0.0 --port 8000 --reload"
convert = "pdm run python -m converter.ssf_to_image"