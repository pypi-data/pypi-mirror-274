[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "fastdev"
version = "0.0.3"
authors = [{ name = "Jianglong Ye", email = "jianglong.yeh@gmail.com" }]
description = "Type less, code more"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.7"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
dependencies = [
    "msgspec[toml,yaml]",
    "rich",
    "fsspec",
    "universal_pathlib",
    "huggingface_hub[cli,hf_transfer]",
    "GitPython",
]

[project.optional-dependencies]
dev = ["build", "twine", "ipython", "ruff", "pytest", "lovely-tensors"]

[project.urls]
Repository = "https://github.com/jianglongye/fastdev"

[project.scripts]
io = "fastdev.fileio.io_cli:main"
