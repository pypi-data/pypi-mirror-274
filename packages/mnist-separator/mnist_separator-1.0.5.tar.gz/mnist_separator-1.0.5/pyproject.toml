[tool.poetry]
name = "mnist-separator"
version = "1.0.5"
description = "mnist-separator is a new technology for recognizing handwritten numbers from the MNIST dataset"
authors = ["Boris Kravtsov <boriskravtsov.contacts@gmail.com>"]
license = "MIT"
readme = "README.md"
packages = [{include = "mnist_separator"}]

[tool.poetry.dependencies]
python = "^3.11"
opencv-python-headless = "^4.9.0.80"
tqdm = "^4.66.4"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
