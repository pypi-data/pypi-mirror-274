from setuptools import setup, find_packages

setup(
    name='predictions_lib_mk',
    version='0.1',
    description='Lib for NN predictions CNN based v1',
    author='Martin Kuba',
    packages=find_packages(),
)