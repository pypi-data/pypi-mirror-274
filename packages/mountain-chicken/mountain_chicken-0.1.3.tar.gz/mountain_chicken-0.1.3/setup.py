from setuptools import setup, find_packages

setup(
    name='mountain_chicken',
    version='0.1.3',
    packages=find_packages(),
    install_requires=[
        #e.g. 'numpy>=1.11.1'
    ],
)