[build-system]
# Specifies the minimum requirements needed to build this project
requires = [
    "setuptools>=42",  # Updated to a newer version for improved package metadata support
    "wheel"            # Needed to build wheels
]

# Specifies the backend that will be used for the build (this tells the tooling how to build your project)
build-backend = "setuptools.build_meta"
