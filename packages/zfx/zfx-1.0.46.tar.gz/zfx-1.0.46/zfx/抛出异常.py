def 抛出异常_Exception():
    """
    抛出一个通用异常（Exception）。

    Raises:
        Exception: 通用异常。
    """
    raise Exception("这是一个通用异常")


def 抛出异常_AttributeError():
    """
    抛出一个属性错误异常（AttributeError）。

    Raises:
        AttributeError: 属性错误异常。
    """
    raise AttributeError("这是一个 AttributeError 异常")


def 抛出异常_IndexError():
    """
    抛出一个索引错误异常（IndexError）。

    Raises:
        IndexError: 索引错误异常。
    """
    raise IndexError("这是一个 IndexError 异常")


def 抛出异常_KeyError():
    """
    抛出一个键错误异常（KeyError）。

    Raises:
        KeyError: 键错误异常。
    """
    raise KeyError("这是一个 KeyError 异常")


def 抛出异常_SyntaxError():
    """
    抛出一个语法错误异常（SyntaxError）。

    Raises:
        SyntaxError: 语法错误异常。
    """
    raise SyntaxError("这是一个 SyntaxError 异常")


def 抛出异常_TypeError():
    """
    抛出一个类型错误异常（TypeError）。

    Raises:
        TypeError: 类型错误异常。
    """
    raise TypeError("这是一个 TypeError 异常")


def 抛出异常_ValueError():
    """
    抛出一个值错误异常（ValueError）。

    Raises:
        ValueError: 值错误异常。
    """
    raise ValueError("这是一个 ValueError 异常")


def 抛出异常_ZeroDivisionError():
    """
    抛出一个零除错误异常（ZeroDivisionError）。

    Raises:
        ZeroDivisionError: 零除错误异常。
    """
    raise ZeroDivisionError("这是一个 ZeroDivisionError 异常")


def 抛出异常_FileNotFoundError():
    """
    抛出一个文件未找到错误异常（FileNotFoundError）。

    Raises:
        FileNotFoundError: 文件未找到错误异常。
    """
    raise FileNotFoundError("这是一个 FileNotFoundError 异常")


def 抛出异常_IOError():
    """
    抛出一个IO错误异常（IOError）。

    Raises:
        IOError: IO 错误异常。
    """
    raise IOError("这是一个 IOError 异常")


def 抛出异常_OSError():
    """
    抛出一个操作系统错误异常（OSError）。

    Raises:
        OSError: 操作系统错误异常。
    """
    raise OSError("这是一个 OSError 异常")


def 抛出异常_ImportError():
    """
    抛出一个导入错误异常（ImportError）。

    Raises:
        ImportError: 导入错误异常。
    """
    raise ImportError("这是一个 ImportError 异常")


def 抛出异常_ModuleNotFoundError():
    """
    抛出一个模块未找到错误异常（ModuleNotFoundError）。

    Raises:
        ModuleNotFoundError: 模块未找到错误异常。
    """
    raise ModuleNotFoundError("这是一个 ModuleNotFoundError 异常")
