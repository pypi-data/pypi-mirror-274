from setuptools import setup, find_packages
setup(
    name='halina_mypackage', #same name as directory
    author='halina',
    version='0.0.2',
    packages=find_packages(),
    install_requires=[],
)
